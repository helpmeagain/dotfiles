# Instalação

1) Rodar: `sudo pacman -S docker lazydocker`
2) Adicionar ao grupo (para não precisar usar sudo):
    - `sudo groupadd docker`
    - `sudo usermod -aG docker $USER`
3) Iniciar docker: `systemctl start docker`

